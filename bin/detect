#!/usr/bin/env bash
# bin/detect - Cloud Native Buildpack detection script
# Always detects (unconditional) - user adds this buildpack when they need FFmpeg

set -euo pipefail

echo "ActiveStorage Preview"
exit 0
